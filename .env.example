# Backend HTTP server
BACKEND_PORT=3000

# Container paths used by docker-compose.yml
SQLITE_DB_PATH=/data/smart-scanner.sqlite
UPLOADS_DIR=/uploads

# Runtime mode for local development/demo
NODE_ENV=development
